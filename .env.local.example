# Rename to .env.local and fill in your actual secrets before running the app.
# Resend will respond with 401 errors if this key is missing or invalid.
RESEND_API_KEY=sk_live_replace_me

# Optional: only required if you plan to enable Supabase features in page.tsx
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
